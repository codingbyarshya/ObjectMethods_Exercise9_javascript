const person={
firstName: "Mario",
lastName: "Rossi",
age: 25
};
const keys=Object.keys(person);
console.log(keys);
const values=Object.values(person);
console.log(values);
//in this exercise i used both Object.keys and also Object.values as the name they have, one of them give keys of the object for output 
//the next one give values of the object for output.
